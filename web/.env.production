# Public client config — the publishable key is designed to ship in the
# browser bundle (RLS is what protects the data), so committing this is safe.
# Secrets (DB password, access tokens, SMTP keys) never go in this file.
VITE_SUPABASE_URL=https://nczxyombguocejgurwop.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_jcJJSw7nfMx3e3iQwprRnA_qnk4bVJI
